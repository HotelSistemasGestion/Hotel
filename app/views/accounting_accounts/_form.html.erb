<%= form_for(@accounting_account, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @accounting_account.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@accounting_account.errors.count, "error") %> prohibited this accounting_account from being saved:</h4>

      <ul>
      <% @accounting_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :cuenta, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :cuenta, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :grupo, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :grupo, class: "form-control" %>
    </div>
  </div>


  <div class="form-group">
    <%= f.label :nombre, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :nombre, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :imputable, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.check_box :imputable, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
