<%= form_for(@accounting_x_entry, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @accounting_x_entry.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@accounting_x_entry.errors.count, "error") %> prohibited this accounting_x_entry from being saved:</h4>

      <ul>
      <% @accounting_x_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :monto, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :monto, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :observacion, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :observacion, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
