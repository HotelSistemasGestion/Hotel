<%= form_for(@accounting_x_automatic_entry, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @accounting_x_automatic_entry.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@accounting_x_automatic_entry.errors.count, "error") %> prohibited this accounting_x_automatic_entry from being saved:</h4>

      <ul>
      <% @accounting_x_automatic_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
