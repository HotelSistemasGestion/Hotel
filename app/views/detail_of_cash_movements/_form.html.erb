<%= form_for(@detail_of_cash_movement, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @detail_of_cash_movement.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@detail_of_cash_movement.errors.count, "error") %> prohibited this detail_of_cash_movement from being saved:</h4>

      <ul>
      <% @detail_of_cash_movement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :sub_monto, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :sub_monto, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
