<div class="box box-primary">
  <div class="box-header with-border">
    <center><h3 class="box-title">Auditoria</h3></center>
    <button type="button" class="btn btn-primary" id="showmenu">
          <span class="glyphicon glyphicon-search"></span>
      </button>
    <div class="menu well" style="display: none;">

    <div class="row">
      <%= form_for_filterrific @filterrific do |f| %>

          <div class="col-xs-3">
          <%= f.text_field(:sorted_by,class: 'filterrific-periodically-observed form-control',placeholder:"Buscar por Tabla")%>
          </div>

          <div class="col-xs-3">
          <%= f.text_field(:sorted_by_nombre,class: 'filterrific-periodically-observed form-control',placeholder:"Buscar por Usuario")%>
          </div>

          <div class="col-xs-3">
          <%= f.text_field(:created_at_lt, class: 'r_datepicker_complaint_lt  form-control',placeholder:"Buscar por fecha")%>
          </div>
           
         <%= render_filterrific_spinner %>
      <% end %>
      </div>
    </div>  
  </div>

  <%= render(
  partial: 'audit/list',
  locals: { audited_tables: @audited_tables }
  ) %>
  

</div>

<script type="text/javascript">
  $(document).ready(function() {
        $('#showmenu').click(function() {
                $('.menu').toggle("slide");
        });
    });
</script>