<%= form_for(@closing_cash, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @closing_cash.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@closing_cash.errors.count, "error") %> prohibited this closing_cash from being saved:</h4>

      <ul>
      <% @closing_cash.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :fecha_cierre, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.date_select :fecha_cierre, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :monto_efectivo, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :monto_efectivo, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :monto_cheque, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :monto_cheque, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :monto_tcredito, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :monto_tcredito, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :monto_tdebito, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :monto_tdebito, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
