<div class="box">
  <div class="box-body">
    <div class="row">
          <%= form_for_filterrific @filterrific do |f| %>
          <div class="col-xs-3">
          <%= f.text_field(:created_at_gte,class: 'filterrific-periodically-observed form-control datepicker',placeholder:"Desde")%>
          </div>

          <div class="col-xs-3">
          <%= f.text_field(:created_at_lt,class: 'filterrific-periodically-observed form-control datepicker',placeholder:"Hasta")%>
          </div>
           
         <%= render_filterrific_spinner %>
      <% end %>
      </div>
  </div>
</div>

<div class="box">
  <div class="box-header with-border">
    <div class="col-sm-4"></div>
          <h3 class="box-title col-sm-4">
            Cierres de Cajas
          </h3>
          <%= link_to 'Nuevo Cierre', new_closing_cash_path, class: "btn btn-primary" %>
  <%= render(
  partial: 'closing_cashes/list',
  locals: { closing_cashes: @closing_cashes }
) %>    
  </div>
  
</div>
<script type="text/javascript">
  $(".datepicker").datepicker({
                format: 'dd-mm-yyyy',
                language: 'es',
              });
</script>        

