<% if @room.errors.blank? %>
	$('#product-modal').modal('hide');
	//$('table').append(' <%= j render "row", room: @room %>');	
	alert("Habitación Creada");
  location.href = "/rooms"
<% else %>
    $('#product-modal').modal('hide');
  	
  	//alert('<%= @room.errors.full_messages[0] %>');
  

  	$('<%= j render "form", room: @room, title: "Nueva Habitacion" %>').on('hidden.bs.modal', function (e) {	
  $(this).remove();
  $('.modal-backdrop').remove();
  }).modal();


<% end %>